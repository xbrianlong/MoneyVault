package main

import (
	"github.com/xbrianlong/MoneyVault/internal/api"
)

func main() {
	// Load env vars
	// if err :=

	// Create Dependencies

	// Initialize Router
	router := api.Router()
	router.Run("localhost:8080")
}
